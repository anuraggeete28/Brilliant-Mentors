import React, { useEffect, useContext } from 'react';
import { View, StatusBar, Text, Linking, Platform, ImageBackground, Image } from 'react-native';

const SplashScreen = () =>{

    useEffect(()=>{

    },[])

    return(

        <View>
            <Text>
                splash
            </Text>
        </View>

    );

}
export default SplashScreen;